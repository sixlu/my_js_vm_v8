#include "embedObject.h"

INIT_STATIC_JAVASCRIPTATTRI_NAME(Node,attributes)//)       Returns true if a node has any attributes, otherwise it r    eturns false   2

class Try{

DECLARE_STATIC_JAVASCRIPT_ATTRI_NAME(attributes);
}
